import redSphere from './red-sphere';
import shapes from './shapes';
import simpleParticles from './simple-particles';

window.addEventListener('DOMContentLoaded', function () {
  // redSphere(window.innerWidth, window.innerHeight);
  // shapes(window.innerWidth, window.innerHeight);
  simpleParticles(window.innerWidth, window.innerHeight);
});
